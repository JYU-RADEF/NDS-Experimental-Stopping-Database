# Changelog

## Unreleased

- Added a cached data access API for the bundled stopping power tables and reference metadata.
- Exported `get_stopping_power_data`, `get_stopping_power_references`, and `clear_stopping_data_cache` from the package root.